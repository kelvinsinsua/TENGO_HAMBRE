elastica:
    resource: "@ElasticaBundle/Controller/"
    type:     annotation
    prefix:   /

order:
    resource: "@OrderBundle/Controller/"
    type:     annotation
    prefix:   /

parameters:
    resource: "@ParametersBundle/Controller/"
    type:     annotation
    prefix:   /

restaurant:
    resource: "@RestaurantBundle/Controller/"
    type:     annotation
    prefix:   /

api:
    resource: "."
    type:     "api"
    prefix:   "/api"

api_login_check:
    path: /api/login_check

NelmioApiDocBundle:
    resource: "@NelmioApiDocBundle/Resources/config/routing.yml"
    prefix:   "/doc"

fos_user:
    resource: "@UserBundle/Resources/config/routing/all.yml"
    
vnzla_states:
    resource: "@VnzlaStatesBundle/Controller/"
    type:     annotation
    prefix:   /